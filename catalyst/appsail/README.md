# Catalyst AppSail

Option for Docker / managed OCI runtime deployment of the backend.

**TODO:**

- Dockerfile for AppSail custom OCI runtime (if chosen over Functions)
- Health check path: `/health`
- Environment variable mapping from `.env.example`
