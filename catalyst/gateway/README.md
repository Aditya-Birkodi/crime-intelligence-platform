# Catalyst API Gateway

Front Functions / AppSail with throttling and auth.

**TODO:**

- Declare routes for `/api/v1/*` and `/health`
- Attach Catalyst Authentication policies
- Configure rate limits and API keys
